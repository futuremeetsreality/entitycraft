# Restore state

When a rule triggers, EntityCraft creates a temporary Home Assistant scene from the selected entity targets. When the rule resets with `restore_previous`, that scene is activated to restore the earlier state.
