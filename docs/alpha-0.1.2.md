# Alpha 0.1.2

EntityCraft now snapshots selected entity targets before applying the trigger action and can restore that snapshot when the rule resets. Existing rules can be edited through the Home Assistant Configure button.
